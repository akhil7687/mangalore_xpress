<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : 'MangaloreXpress' %> </title>
    <meta content='width=device-width, initial-scale=1' name='viewport'>
    <%= csrf_meta_tags %>
    <link href="<%=asset_path('aff-logo.ico')%>" rel='icon'>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=Lato|Poppins%7CQuicksand:500,700" rel="stylesheet">
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag Ckeditor.cdn_url%>
  </head>

  <body>
    <%if request.user_agent !~ /MANGALOREXPRESS/%>
      <%=render 'layouts/navbar'%>
    <%end%>
    <%=render 'shared/flash'%>
    <%= yield %>
    <div id="modal_container"></div>
    <%if request.user_agent !~ /MANGALOREXPRESS/%>
      <%=render 'home/footer'%>
    <%end%>
  </body>
</html>
